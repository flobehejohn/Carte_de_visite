/**
 * ARCHIVE (Phase 3)
 * Ce fichier gérait l'ancien système de Sprites Typographiques 2D.
 * Il est neutralisé (Stubs) car l'affichage est désormais gouverné par
 * OrbTextManager.js via Troika-Three-Text (SDF).
 * Il sera supprimé dès la refactorisation de RitualOrchestrator.js.
 */

export function setOrbTextConfig(ctx, config = {}) {}
export function buildOrbText(ctx) {}
export function updateOrbTextForFrame(ctx, progress, chaos) {}
